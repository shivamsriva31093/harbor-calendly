<div>
    <h2>Event Update</h2>
    <p>Hello {{userName}},</p>
    <p>The event "{{eventTitle}}" has been updated.</p>
    <div>
        <h3>Changes:</h3>
        <pre>{{changes}}</pre>
    </div>
    {{#if requiresResponse}}
    <p><strong>Note:</strong> Due to time changes, your response has been reset. Please respond again:</p>
    <div style="margin-top: 20px;">
        <a href="{{baseUrl}}/events/{{eventId}}/respond?response=ACCEPTED">Accept New Time</a> |
        <a href="{{baseUrl}}/events/{{eventId}}/respond?response=DECLINED">Decline New Time</a>
    </div>
    {{/if}}
</div>
